pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                // Correctly checking out the code from the repository
                checkout scm
            }
        }
        // Add more stages for your pipeline as needed
    }
}
